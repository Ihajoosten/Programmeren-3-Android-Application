package com.example.element.adapter;

import com.example.element.models.Element;

public interface ElementAdapter {

    //Hoeft geen public want het is een interface
    //Voegt de blindwall toe aan de adapter
    void elementAdapter(Element element);
}
